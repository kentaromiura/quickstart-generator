language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-quickstart-generator' ]; then cd .. && eval "mv $currentfolder generator-quickstart-generator" && cd generator-quickstart-generator; fi

